.row
  %h2 Editing #{link_to @quiz.name, edit_quiz_path(@quiz)}
  = simple_form_for(@question, :url=>question_path(@question), :html=>{method: :put, class: "form-vertical"}) do |f|
    = render 'form', f: f

= render :partial=>"quizzes/reveal_question", :locals=>{:qq=>@question, :hide_navigation=>false}
